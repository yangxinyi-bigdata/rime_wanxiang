
1. [x] 发送对话之后的回车,变成可配置
2. [x] ai对话的名称,更加合理的显示
3. [x] 提示词的修改功能还没有实现.

### 试一下unhandlekey能不能监控无输入状态的按键
可以通过监控ctrl这几个按键来实现和服务器的数据同步的功能,所以在非输入状态下颌服务器的配置同步也是完全可以做到的！
如果以后有需要的话，可以搞一下，现在看起来没什么必要。

1. [x] 发送对话之后的回车,变成可配置
首先配置中读入这个配置
然后判断配置是什么,决定发送的response_key参数是什么
- [x] AI回复之后的按键,在非自动上屏状态下也可以触发


1. [x] ai对话的名称,更加合理的显示

在"AI助手管理"页面中,当修改了

- [x] 让配置的更新可以生成命令,更新配置